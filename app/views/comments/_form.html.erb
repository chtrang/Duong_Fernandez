<%= form_for [@book, @comment] do |f| %>

	<%= f.hidden_field :book_id %>
	<%= f.text_field :user_name, :placeholder => "user name" %>
	<br />
	<%= f.text_area :message %>
	<br />
	<%= f.submit %>
<% end %>
