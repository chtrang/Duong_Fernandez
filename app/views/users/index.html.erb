<h1>Listing users</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |f| %>
      <tr>
        <td><%= f.email %></td>
         <td><%= link_to 'Show', f %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
